# POST-11 — Web app v2 (deferred)

Mobile is the MVP client. Web v2 should:

- Share `POST /api/chat`, auth, and rate limits with mobile
- Not revive `feature/frontend-foundation` without audit

Start after `feature/auth-production` and MVP stack are on `main`.

Branch name when started: `feature/web-app-v2`
